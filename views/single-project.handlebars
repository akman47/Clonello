{{!-- manage project modal --}}
<section class="modal modal-invite-user">
        <div class="invite-user">
            <h2>Manage Project</h2>
            <br>
            <form class="add-user-form" data-project-id="{{project.id}}">
                <div class="form-input user-menu">
                    <label for="invite-user-menu">Choose a user to invite: </label>
                    <select name="invite-user-menu" id="invite-user-menu">
                        {{#each users}}
                        <option value="{{username}}-id{{id}}">{{username}}</option>
                        {{/each}}
                    </select>
                </div>
                <div class="btn-container">
                    <button type="submit" class="btn btn-invite-user btn-go modal-btn">Add User</button><br>
                    <button type="button" class="btn btn-danger modal-btn" id="delete-project-btn">Delete Project</button><br>
                    <button type="button" class="btn btn-close-invite-modal modal-btn">Close</button>
                </div>
            </form>
        </div>
</section>

{{!-- add task modal --}}
<section class="modal modal-add-task">
        <div class="add-task">
            <h2>New Task</h2>
            <br>
            <form class="new-task-form" data-project-id="{{project.id}}">
                <div class="form-input">
                    <input type="text" id="new-task" name="new-task" placeholder="Describe your task..." />
                </div>
                <br>
                <div class="form-input user-menu">
                    <select name="user-menu" id="task-user-menu">
                        <option disabled selected value>Assign To Project Member: </option>
                        {{#each project.users}}
                        <option value="{{username}}-id{{id}}">{{username}}</option>
                        {{/each}}
                    </select>
                </div>
                <br>
                <div class="form-input status-menu">
                    <select name="status-menu" id="status-menu">
                        <option disabled selected value>Select Task Status:</option>
                        {{#each status}}
                        <option value="{{title}}-id{{id}}">{{title}}</option>
                        {{/each}}
                    </select>
                </div>
                <br>
                <div class="btn-container">
                    <button type="submit" class="btn btn-go modal-btn" id="btn-add-task">Add Task</button>
                    <button type="button" class="btn modal-btn" id="close-task-modal">Close</button>
                </div>
            </form>
        </div>
    </div>
</section>

{{!-- edit status modal --}}
<section class="modal modal-edit-status">
    <div class="edit-status">
        <h2>Manage Status</h2>
        <br>
        <form class="new-status">
            <h3>Add</h3>
            <div>
                <input type="text" id="new-status-text" name="new-status-text" placeholder="To Do" />
            </div>
            <div class="btn-container">
                <button type="click" id="btn-add-status" class="btn btn-go modal-btn">Add</button>
            </div>
        </form>
        <form class="delete">
            <h3>Delete</h3>
            <select name="delete-status-menu" id="status-menu">
                <option disabled selected value>Select Task Status</option>
                    {{#each status}}
                    <option value="{{title}}-id{{id}}">{{title}}</option>
                    {{/each}}
            </select>
            <br>
            <div class="btn-container">
                <button type="click" id="btn-delete-status" class="btn btn-danger modal-btn">Delete</button><br>
                <button type="button" class="btn modal-btn" id="close-status-modal">Close</button>
            </div>
        </form>
    </div>
</section>

{{!-- main single-project view --}}
<div>
    <a class="back" href="/dashboard"> &larr; Back to Dashboard</a>
    <div class="project-header">
        <div class="header-el">
            <ul class="member-list">
                <h2>Members:</h2>
                {{#each project.users}}
                    <li>{{username}}</li>
                {{/each}}
            </ul>
        </div>
        <div class="header-el">
            <h2 class="project-title-display">{{project.title}}</h2>
        </div>
        <div class="header-el">
            <button type="button" class="btn add-task-btn" id="open-task-modal">Add Task</button><br>
            <button type="button" class="btn" id="open-status-modal">Edit Status</button><br>
            <button type="button" class="btn btn-open-invite-modal" id="invite-btn">Edit Project</button><br>
        </div>
    </div>
    {{!-- <div class="add-task-div">
        <button type="button" class="btn add-task-btn" id="open-task-modal">Add Task</button>
        <button type="button" class="btn" id="open-status-modal">Add Status</button><br>
    </div> --}}
    <div class="task-display">
        {{#each project.tasks}}
        <div class="card">
            <p id="task-title" class="task-text-{{id}}" data-task-id="{{id}}">{{task_text}}</p>
            <p class="task-user" data-user-id="{{user.id}}">Assigned To: {{user.username}}</p>
            <p class="task-status" data-status-id="{{status.id}}">Status: {{status.title}}</p>
            <div class="task-btns">
                <button type="button" class="btn btn-edit" data-task-id="{{id}}">Edit Task</button>
                <button type="button" class="btn btn-delete" data-task-id="{{id}}">Delete Task</button>
            </div>
        </div>
        {{/each}}
    </div>
</section>

<script src="/javascript/invite-user.js"></script>
<script src="/javascript/add-task.js"></script>
<script src="/javascript/delete-task.js"></script>
<script src="/javascript/delete-project.js"></script>
<script src="/javascript/edit-status.js"></script>